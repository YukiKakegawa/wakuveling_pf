Home
全ユーザーの投稿とランキング表示

<div class="container">
  <div class="row">
    <div class="col-sm-9">
      投稿
    </div>
    <div class="col-sm-3">
      <p>ランキング</p>
      <% @posts.each do |post| %>
      <%= link_to post_path(post) do %>
      <%= attachment_image_tag post, :image, :fill, 100, 100, format: 'jpeg' %>
      <% end %>
    <div>
      <%= post.tourist_spot %>
    </div>
    <div>
      <%= post.prefecture %>
    </div>
      <% end %>
    </div>
  </div>
</div>