<div class="container mt-5">
  <div class="row">
    <div class="col-sm-10">
      <div class="row">
      <% @posts.each do |post| %>
        <div class="col-sm-4 mb-4">
          <div class="card">
            <div class="card-img mb-2">
            <%= attachment_image_tag post, :image, size: "285x300", format: 'jpeg' %>
            </div>
            <div class="d-flex justify-content-around">
              <h5 class="card-title"><%= post.tourist_spot %></h5>
              <div class="card-text"><%= post.prefecture %></div>
              <%= link_to "詳細", post_path(post), class: "btn btn-secondary mb-1"  %>
            </div>
          </div>
        </div>
     <% end %>
     </div>
    </div>

    <div class="col-sm-2 text-center">
      <p>いいねランキング TOP5</p>
      <% @good_ranking_posts.each.with_index(1) do |post,i| %>


    <!--<%#=@all_ranks.each.with_index(5) do |note, i| %>-->
          第
        <%= i %>
          位
    <!--      = note.title-->
    <!--    <%#end %>-->

      <div class="container-ranking">
        <div class="card mb-1">
          <%= link_to post_path(post) do %>
          <%= attachment_image_tag post, :image, size: "158x150", format: 'jpeg' %>
          <% end %>
            <div>
              <%= post.tourist_spot %>
            </div>
            <div>
              <%= post.prefecture %>
            </div>
        </div>
      </div>
      <% end %>
    </div>
    </div>
    </div>





